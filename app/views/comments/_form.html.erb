<div class="col-md-8 col-md-offset-2">
  <%= form_for [@report, comment], remote: true do |f| %>
    <div class="form-group">
      <%= f.text_area :text, rows: 5, class: 'form-control', placeholder: 'Add a comment...' %>
    </div>
    <%= f.submit class: "btn btn-default" %>
  <% end %>
</div>
