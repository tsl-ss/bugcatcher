<table class="table table-hover" id="leaderboardTable">
  <thead>
    <th>Score</th>
    <th>Player Name</th>
  </thead>
  <% @leaderboard.each do |entry| %>
    <tr>
      <td><%= entry.score %></td>
      <td><%= entry.author.email %></td>
    </tr>
  <% end %>
</table>
